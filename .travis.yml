language: c
sudo: false
jobs:
  include:
    - name: "Linux GCC"
      os: linux
      compiler: gcc
    - name: "Linux Clang"
      os: linux
      compiler: clang
#    - name: "Mac OS X Clang"
#      os: osx
#      compiler: clang

before_install:
  - sudo apt-get -y install doxygen
  
script:
  - mkdir -p build_result
  - make install PREFIX=build_result
